# Core LLM
anthropic>=0.49.0

# Vector store + embeddings
chromadb>=0.5.0
sentence-transformers>=3.0.0
# Pin transformers to the 4.x line: transformers 5.x needs torch >= 2.8, which
# breaks on common pre-installed CUDA torch builds (e.g. 2.6.0+cu118).
transformers>=4.41,<5

# HTTP + parsing
requests>=2.31.0

# UI (Streamlit demo)
streamlit>=1.36.0

# Web backend (custom site)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Config
python-dotenv>=1.0.0
