# Core LLM
anthropic>=0.49.0

# Vector store + embeddings.
# Embeddings use Chroma's built-in ONNX MiniLM model (via onnxruntime) — light
# enough for a 512MB host. No PyTorch / sentence-transformers needed at runtime.
chromadb>=0.5.0
onnxruntime>=1.17.0

# HTTP + parsing
requests>=2.31.0

# UI (Streamlit demo)
streamlit>=1.36.0

# Web backend (custom site)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Config
python-dotenv>=1.0.0
